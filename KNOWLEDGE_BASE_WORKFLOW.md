# CBST 课程知识库维护说明

## 用途

本知识库为 CBST 对话、CBST 逻辑字词训练、认知谬误识别及后续技能模块提供可追溯的专业上下文。它不是模型的永久记忆；模型在每次生成题目、点评或分析前，应检索与当前任务相关的课程条目。

## 更新方式

当需要导入新资料时，在 Codex 对话中上传课件，并明确说明：

`请学习这些课件，更新知识库。`

随后应完成以下步骤：

1. 提取课件中的术语、逻辑字词规则、案例、练习目标、限制条件和来源信息。
2. 生成可检索的知识条目，并保留课件名称、章节或页码、更新时间。
3. 自动推荐资料适用模块。
4. 由产品维护者确认或修改模块范围后，再将条目作为运行时依据。
5. 对新条目进行样例验证，确保不会改变既有模块的核心规则。

## 条目建议结构

- `source`：课件或资料名称。
- `section`：章节、页码或主题。
- `modules`：适用训练模块。
- `topic`：例如“部分逻辑”“时间逻辑”“共情顺序”。
- `rule`：可供模型执行的简明规则。
- `example`：课件案例或经审核变体。
- `citationLabel`：面向用户展示的简短课程依据。
- `updatedAt`：更新时间。

## 使用边界

- 资料仅在被确认的模块范围内参与模型提示词。
- 用户侧显示简短课程依据，避免展示冗长原文。
- 资料之间出现实际规则冲突时，暂停自动合并，由维护者确认处理方式。
- 本知识库用于训练与学习，不用于心理诊断、治疗建议或医疗意见。
